[package]
name = "stellium-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
stellium-escrow = { path = "../contracts/escrow" }
stellium-payment = { path = "../contracts/payment" }
